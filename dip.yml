version: "8.0"

environment:
  RAILS_ENV: development
  BUNDLE_DEPLOYMENT: "false"
  BUNDLE_WITHOUT: ""

compose:
  files:
    - docker-compose.dev.yml
  project_name: openbill-admin-dip

interaction:
  shell:
    description: Open a Bash shell in app container
    service: app
    command: bash
    compose:
      run_options: [no-deps]

  bundle:
    description: Run Bundler commands
    service: app
    command: bundle

  yarn:
    description: Run Yarn commands
    service: app
    command: yarn

  rails:
    description: Run Rails commands
    service: app
    command: bundle exec rails
    environment:
      DATABASE_HOST: postgres
      DATABASE_USER: postgres
      DATABASE_PASS: postgres
      DATABASE_NAME: openbill_development
      DATABASE_NAME_TEST: openbill_test
      DATABASE_POOL: 5

  rake:
    description: Run Rake commands
    service: app
    command: bundle exec rake
    environment:
      DATABASE_HOST: postgres
      DATABASE_USER: postgres
      DATABASE_PASS: postgres
      DATABASE_NAME: openbill_development
      DATABASE_NAME_TEST: openbill_test
      DATABASE_POOL: 5

  rspec:
    description: Run RSpec tests
    service: app
    command: bundle exec rspec
    environment:
      RAILS_ENV: test
      DATABASE_HOST: postgres
      DATABASE_USER: postgres
      DATABASE_PASS: postgres
      DATABASE_NAME: openbill_development
      DATABASE_NAME_TEST: openbill_test
      DATABASE_POOL: 5
    compose:
      run_options:
        - -e RAILS_ENV=test

  server:
    description: Start the application in Docker
    service: app
    compose:
      method: up

provision:
  - dip compose up -d postgres
  - dip compose exec postgres pg_isready -U postgres
  - dip bundle install
  - dip yarn install
  - dip rake db:create
  - dip rake db:migrate
  - dip rake db:create RAILS_ENV=test
  - dip rake db:migrate RAILS_ENV=test
